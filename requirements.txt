# compressor_2 evolution dependencies
#
# 1. Python deps (this file):
#    pip install -r requirements.txt
#
# 2. System packages (pdflatex, pdftoppm, libnuma for SGLang): see requirements-system.txt
#    Debian/Ubuntu: xargs -a requirements-system.txt sudo apt-get install -y
#    (Required for evolution tree PDF/PNG; SGLang needs libnuma1.)

deap
joblib
kconfiglib
matplotlib
numpy
openai
optuna
pyyaml
requests
scikit-learn
scipy
sentence-transformers
sgl_kernel
sglang
transformers

# Optional (install for specific zero-order methods):
# cma               # cmaes
# optuna            # optuna_tpe (now in main deps)
# smac              # smac_opt (+ ConfigSpace)
# ConfigSpace       # smac_opt
# scikit-optimize   # skopt_opt
# pdfo              # tr_dfo (BOBYQA/NEWUOA)
# pybobyqa          # tr_dfo alternative
