# System packages required for compressor_2 evolution (Debian/Ubuntu).
# Install: make install_requirements (or see requirements.txt)
# Run: apt-get update && apt-get install -y $(xargs -a requirements-system.txt)
#
# Build deps for smac/ConfigSpace (swig, python3-dev, g++, build-essential)
swig
build-essential
python3-dev
g++
#
# SGLang runtime
libnuma1
#
# Evolution tree: compile evolution_tree.tex -> PDF -> PNG
# (pdflatex + TikZ/standalone from texlive; pdftoppm for PNG)
texlive-latex-extra
poppler-utils
